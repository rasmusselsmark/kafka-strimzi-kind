apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kafka-services-ingress
  namespace: kafka
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
spec:
  ingressClassName: nginx
  rules:
  # Redpanda Console - access via http://console.127.0.0.1.nip.io
  - host: console.127.0.0.1.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: redpanda-console
            port:
              number: 8080
---
# Prometheus runs in the monitoring namespace, and an Ingress can only route to
# services in its own namespace, so it needs a separate Ingress resource
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: monitoring-services-ingress
  namespace: monitoring
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
spec:
  ingressClassName: nginx
  rules:
  # Prometheus - access via http://prometheus.127.0.0.1.nip.io
  - host: prometheus.127.0.0.1.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: prometheus-operator-kube-p-prometheus
            port:
              number: 9090
